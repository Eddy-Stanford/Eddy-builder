name: Test GNU Build & Deploy

on:
  pull-request:
    branches: [ "main" ]
jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file gnu.Dockerfile -t eddy-builder-gnu

